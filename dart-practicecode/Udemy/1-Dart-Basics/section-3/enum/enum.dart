
enum colors {red, yellow, orange, blue, green, black, purple, white}
void main() {
 //we can declare the enum in the scope of main function
  print(colors);
  print(colors.values);
  print(colors.green);
}